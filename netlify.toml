# Netlify deploy (app is at the repo root).
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every route serves the app shell.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
